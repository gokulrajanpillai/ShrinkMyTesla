{
  "name": "ShrinkMyTesla (Ubuntu 22.04)",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ]
    }
  },
  "postCreateCommand": "echo 'Todo: npm ci && pip install -r requirements.txt || true'",
  "remoteUser": "vscode"
}